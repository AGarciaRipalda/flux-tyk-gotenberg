apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gotenberg-manager-monitor
  namespace: gotenberg-manager
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: gotenberg-manager
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
